# 里程碑验收报告 · resolution_rate

## 0. 基本信息

- **姓名 / 学号**：庄昭雄 / （无学号）
- **仓库链接**：https://github.com/173787247/omnisupport-copilot/tree/homework/resolution-rate-zhuangzhaoxiong
- **选做指标**：`resolution_rate`（工单解决率）
- **完成档位**：阶段三（六个阶段全打通 + 端到端演示 + 越权反例 + 时间旅行/性能基线证据齐全）

## 1. 指标口径（阶段一）

- **分子**：`status ∈ {resolved, closed}` 的工单数（staging `is_resolved` → intermediate `resolved_ticket_count`）
- **分母**：当天 **创建** 的工单数（`created_date` / `metric_date` 口径的 `ticket_count`），不是当天活跃/处理量
- **公式**：`resolved_ticket_count / nullif(ticket_count, 0)`
- **维度**：`metric_date × product_line`（另可切 `priority` / `org_id` / `category`）
- **PII 等级与理由**：聚合指标本身 **none**；Agent 视图禁止 `customer_id` 等标识字段，仅安全维度 + 指标值
- **可查角色 / HITL**：`support_ops` / `instructor` / `admin`；越权或注册表外请求直接拒绝并审计；禁止裸 SQL 访问原始工单表

详细边界见：`docs/blueprints/milestone_resolution_rate_acceptance.md`

## 2. 契约门禁（阶段二）

- **用到的字段与枚举**：
  - `status`: `open | pending | in_progress | resolved | closed | escalated`
  - `product_line`: `northstar_workspace | northstar_edge_gateway | northstar_studio | cross_product`
  - `created_at`（时间口径）、`org_id` / `priority` / `category`（维度）
- **拦截演示证据**：未知指标 `raw_sql_metric` → `METRIC_DENIED`；角色 `viewer` → `ROLE_DENIED`（见阶段六）
- **契约文件**：`contracts/data/ticket_contract.json`、`contracts/tools/tools/query_support_kpis_v1.json`（`maxItems` 扩至 12）

## 3. 入湖与补数（阶段三）

- **制造的缺口**：删除分区日 `2026-04-20` 的工单（`ticket_fact` 500 → 491，同步删 bronze 9 条）
  - 证据：`reports/week03/gap_manufacture.json`
- **补数命令与结果**：
  - dry-run：`python -m pipelines.ingestion.replay_backfill --mode backfill ... --dry-run`
  - execute：补回后 bronze 新写入 9，其余 491 走 duplicate；再全量重跑 `Bronze new: 0 / Duplicates: 500`
  - 证据：`reports/week03/recovery_decision_log.json`、`recovery_execute_log.json`、`ticket_ingest_idempotent_rerun.json`
- **幂等如何保证**：`raw_ticket_event` 按 `source_id + source_fingerprint` 唯一约束；重复 ingest 只记 duplicate，不翻倍写 bronze；silver 对 `ticket_id` upsert

## 4. 时间旅行与基线（阶段四）

- **补数前/后快照 id**（`silver.ticket_fact`）：
  - 补数前（491 行）：`3705855403373275092`
  - 补数后（500 行）：`5804128467664335385`
  - 证据：`reports/week04/snapshots_before_after.json`、`reports/week04/time_travel_demo_report.md`
- **性能基线关键数字**（自然状态，未 compaction）：
  - `silver.ticket_fact`：rows=500，snapshots=5，files=1，avg_file_size≈28748，latest_snapshot_id=`5804128467664335385`（后续 status flip 后又产生新快照 `8415951783898235613`）
  - 证据：`reports/week04/iceberg_baseline_report.md`

## 5. dbt + 注册表 + 工具（阶段五）

- **改动文件清单**：
  - `analytics/models/staging/stg_tickets.sql`（`is_resolved` 含 closed）
  - `analytics/models/intermediate/int_ticket_activity_daily.sql`（新增 `resolution_rate`）
  - `analytics/models/marts/support_kpi_mart.sql` / `agent_tool_input_view.sql` / `schema.yml`
  - `analytics/metric_registry_v1.yml`（登记 ratio 分子分母）
  - `analytics/tests/ratio_metrics_between_0_and_1.sql`
  - `analytics/scripts/validate_metric_registry.py`
  - `contracts/tools/tools/query_support_kpis_v1.json`
  - `tests/integration/test_week05_metric_registry.py`
- **dbt build 结果**：`dbt build --select tag:week05` → **PASS=45 WARN=0 ERROR=0**（含 `resolution_rate` accepted_values / ratio 0–1 / no_pii）
- **Week5 测试结果**：
  - `test_week05_metric_registry.py` + `test_week05_kpi_query_tool.py` + `test_week05_metric_contracts.py` → **12 passed**
- **注册表登记片段**：

```yaml
  - name: resolution_rate
    business_name_zh: "工单解决率"
    metric_type: ratio
    formula: "resolved_ticket_count / ticket_count"
    numerator: resolved_ticket_count
    denominator: ticket_count
    definition_status: production
    allowed_roles: ["support_ops", "instructor", "admin"]
    quality_tests: ["between_0_and_1", "non_negative"]
```

## 6. 端到端演示（阶段六）

演示链路：

1. 将 `TKT-20260812-000311` 从 `in_progress` 改为 `resolved`
2. Iceberg 重新物化产生新快照 `8415951783898235613`
3. `dbt build` 后 `resolution_rate` 可查且值域合法
4. `support_ops`（带 `actor_org_ids`）查询成功：`allowed=true`，`audit_id=c47c82fe-104b-418d-8b44-c5a8cb3140f6`
5. `viewer` 越权被拒：`denial_code=ROLE_DENIED`，`audit_id=5664ad9a-bfff-45b8-953a-04b409283b77`
6. 两次查询均写入 audit（见 JSON 内 `audit` 字段）

- **演示链接 / 证据目录**：
  - `reports/milestone_resolution_rate/e2e_demo.json`
  - `reports/week05/query_resolution_rate_allowed.json`
  - `reports/week05/query_resolution_rate_denied.json`
  - `reports/week05/query_resolution_rate_sample.json`
- **有权限查询结果**：`status=ok`，返回 `resolution_rate` 行（例：`northstar_studio` 在 `2026-04-04` 为 `1.0`）
- **越权 denial_code**：`ROLE_DENIED`

## 7. 我踩的坑 / 我的思考（加分项）

1. **四处白名单缺一不可**：mart values、safe_view、metric_registry、工具契约 `maxItems`；少一处就查不到。
2. **不要把 metric enum 写死进 JSON Schema**：会把「未知指标」提前打成 `SCHEMA_VALIDATION_FAILED`，破坏课程测试期望的 `METRIC_DENIED`；真正闸门应是注册表 + safe_view。
3. **`support_ops` 必须带 `actor_org_ids`**，且查询窗口 ≤ 31 天，否则分别是 `ORG_SCOPE_REQUIRED` / `WINDOW_TOO_LARGE`。
4. **幂等补数的肌肉记忆**：先造缺口 → dry-run 计划 → execute；再全量重跑应看到 duplicate=100%，行数不翻倍。
5. 国内构建慢时，瓶颈多半是 **pip 包**而不是 Docker Hub；Docker 镜像加速与 PyPI 镜像要分开看。
